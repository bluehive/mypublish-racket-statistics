# ボートレース統計入門

Racket（通常言語）とデータフレームライブラリ **「RacketFrames」** を用いて、オープンデータ API（**`turnmark/api`**）からのデータ収集・前処理から統計データの可視化、簡易予測モデルの作成までを学ぶ、文系大学生（プログラミング・統計初心者）向けの入門電子書籍リポジトリです。

---

## 🎯 本書の対象読者
* Scratchを少し触ったことがあり、テキストベースのプログラミングに挑戦したい方。
* RやPythonの前に、シンプルかつ安全な環境（DrRacket）でデータ分析の実利を学びたい方。
* ボートレースのデータを題材に、楽しく実用的な統計学を学びたい方。

---

## 📋 最新版 目次 (Table of Contents)

### 序章：なぜRやPythonではなくRacketで統計を学ぶのか？
* **0.1 統計を学ぶ楽しさとボートレースという題材**
* **0.2 R/Pythonに匹敵するRacketのデータ解析力：「RacketFrames」の紹介**
* **0.3 DrRacketのインストールと最初の準備**

### 第1章：Racketプログラミングの第一歩
* **1.1 ScratchからRacketへ：ブロックとテキストコードの対応**
* **1.2 基本的な計算と関数：DrRacketでの動かし方**
* **1.3 リストとハッシュの基本：データをまとめる方法**
* **1.4 超わかりやすいボートレース（競艇）のルールと仕組み**
  * *【前提】自然環境要素（天候・風向風速・波高）の対象外方針について*
* **1.5 章のまとめ：三角ロジックによる整理**

### 第2章：ボートレースのデータを集める（データ収集）
* **2.1 【本線】オープンデータ API (`turnmark/api`) から日付指定・期間一括 JSON を取得・蓄積してパースする**
  * *目的別タスクランナーメニュー (`today`, `json`, `range`)*
  * *Racket による全 JSON スキャン & 蓄積パース (`code/ch02-json-parser.rkt`)*
* **2.2 【応用・参考】公式Webサイト（HTML生データ）の `curl` 取得とパース**
* **2.3 💡 生データ (HTML) と構造化データ (JSON/CSV) のパイプライン比較**
* **2.4 CSVファイル形式とボートレースデータの構造**
  * *🔥 【統計学の真髄】なぜ「今日だけのデータ」では役に立たないのか？ ＆ 日付列 (`date`) の重要性*
  * *🚀 【ステップバイステップ】過去データと比較分析を進める 4 つの手順*
* **2.5 【深掘り大盛り解説】統計学における「過去データの期間」＝「量」×「品質（鮮度）」の方程式**
  * *ボートレース特有の休催日事情と「期間 (日付)」vs「レース数 (件数 N)」の本線役割分担*
  * *📖 【中盛りコラム】自然環境ノイズを排除し、過去データを積み重ねる統計的・相関分析上の真の意義*
* **2.6 章のまとめ：三角ロジックによる整理**
* **コラム: オープンデータ API `turnmark/api` に学ぶ構造化 JSON スキーマとデータのあり方**

### 第3章：RacketFramesによるデータ操作（データフレーム入門）
* **3.1 RacketFramesの導入：CSVから `DataFrame` へのデータ読み込み**
* **3.2 主要コンポーネント：インデックス（Index）とシリーズ（Series）の役割**
* **3.3 Series（シリーズ）は「縦1列のデータ専門チーム」**
* **3.4 Index（インデックス）の秘密：図書カードと自動位置合わせ**
* **3.5 データフレームのルールと注意点**
* **3.6 欠損値 `RFNoData`（空欄データ）の自動スキップ**
* **3.7 🔥 【重要本線ステップ】期間累積データから「直近 N レース (サンプルサイズ N)」を厳密スライス抽出する (`df-take-recent-races`)**
* **3.8 RacketFramesの設計思想まとめ**
* **3.9 実践：綺麗なデータを抽出してみよう！**
* **3.10 章のまとめ：三角ロジックによる整理**

### 第4章：RacketFramesによる統計と集計（データ分析）
* **4.1 🔥 【大盛り深掘り解説】なぜ「フルイ分け (フィルタリング)」と「箱分け一括集計 (Groupby)」をマスターしなければならないのか？**
  * *① 一般的なデータ分析・ビジネスにおける必要性と有用性（一般的メリット）*
  * *② ボートレースデータ分析における具体的な必要性と有用性（ステップバイステップ実践解説）*
* **4.2 🔥 【大盛り深掘り解説】基本統計量：平均値 (`df-mean`) と標準偏差 (`df-std`) を学ばなければいけない理由とメリット**
  * *① なぜ平均値と標準偏差をセットで学ぶのか？（一般的理由とメリット・「平均の罠」回避）*
  * *② ボートレース統計の観点における具体的な必要性とメリット（安定型レーサー vs ピンロク波乱型レーサー・機力格差の評価）*
  * *③ RacketFrames での計算コード実装 (`df-mean` / `df-std`)*
* **4.3 条件フィルタリング (`df-filter`)：目的のデータだけをすくい取る**
* **4.4 欠損値の除去 (`df-drop-na`)**
* **4.5 グループ化と集約 (`df-groupby`)：箱分けして一括集計**
* **4.6 まとめと次のステップ**
* **4.7 章のまとめ：三角ロジックによる整理**

### 第5章：RacketFramesによるデータの可視化（プロット）
* **5.1 散布図プロット (`df-plot-scatter`)：最推し相関可視化**
  * *① 散布図（Scatter Plot）の出現背景と一般的必要性・メリット（認知限界の打破・外れ値発見）*
  * *② ボートレースにおける具体的な関係（モーター2連対率 vs 確定着順）*
* **5.2 描画結果と相関関係の読み取り**
  * *① 相関関係（Correlation）の背景情報と大盛りメリット（正の相関・負の相関）*
  * *② ボートレースにおける具体的な関係（右下がりの負の相関と機力絶対主義の証明）*
* **5.3 視覚化がもたらす 2 つの深掘り洞察**
  * *万舟券爆発パターンの視覚化 ＆ 第6章への接続*
* **5.5 章のまとめ：三角ロジックによる整理**

### 第6章：予測モデルの作成と検証（実践）
* **6.1 予想モデルの数理設計**
  * *① 多重線形スコアリングモデルの出現背景と大盛りメリット（主観バイアス排除・PDCA改善）*
  * *② ボートレースにおける具体的な関係（枠番＋勝率＋機力の多角統合数式）*
* **6.2 テーブルの結合 (`df-join`)：出走表と過去成績のガッチャンコ**
  * *① Relational Join の背景情報と大盛りメリット（データ補完・ミスゼロ自動照合）*
  * *② ボートレースにおける具体的な関係（出走表 df-today ＋ 過去蓄積データ df-history の結合）*
* **6.3 予想スコアの全自動計算**
* **6.4 予測の検証：実際の結果と答え合わせをしよう！ (`evaluate-model`)**
  * *① モデル検証（Evaluation / Backtesting）の背景情報と大盛りメリット（自己満足の防止・精度改善）*
  * *② ボートレースにおける具体的な関係（最高スコア艇 vs 1着確定艇の照合テスト）*
* **6.5 おわりに：データサイエンスのサイクルを回し切ったあなたへ**
* **6.6 章のまとめ：三角ロジックによる整理**

### 付録
* **付録A：付属ソースコード一覧（ガイド）**
* **付録B：ボートレース用語と統計・RacketFrames用語の対応表 (Glossary)**
* **付録C：DrRacketのデバッグとお助け機能**
* **付録D：`mise` を使った開発環境構築と自動化タスクガイド**
* **付録E：`curl` を使ったデータ収集とコマンドガイド**

---

## 📦 レース結果 JSON データセット（最大約3年対応）

コードは次のレイアウトの日次／月次 JSON を想定しています（会場日次のスキーマは共通）。

| パス | 内容 |
| --- | --- |
| `data/raw/tsu/YYYY-MM-DD.json` | 津（stadium 9）日次 |
| `data/raw/gamagori/YYYY-MM-DD.json` | 蒲郡（stadium 7）日次 |
| `data/national/by_month/YYYY-MM.json` | 全国・月次サマリ |
| `data/national/by_racer_monthly/YYYY-MM.json` | 全国・選手別月次（出走/1着/連対率など） |
| `data/summaries/*_3y.json` | 収集サマリ（任意） |

### 同梱状況（このブランチ）
- 現状 git 追跡しているのは **津のパイロット分**（`data/raw/tsu/`、おおよそ 2026-03-09〜2026-09-08・約半年）です。
- 収集済みの約3年分（2023-09-09〜2026-09-08、津579日 / 蒲郡607日 / 全国月次37ヶ月）は、必要に応じて外部から同じパスへ配置してください。
- 環境変数 `BOATRACE_DATA_ROOT` でデータルートを切り替えできます（未設定時はリポジトリ直下）。

### どこから取得したか
- **取得元**: 非公式の公開 JSON API「[Boatrace Open API](https://github.com/boatraceopenapi/results)」の `results/v3`
- **エンドポイント例**: `https://boatraceopenapi.github.io/results/v3/{YYYY}/{YYYYMMDD}.json`（1日1ファイル・全国24場入り）
- **公式との関係**: BOATRACE 公式サイトとは無関係のコミュニティ公開データです。欠損や差異があり得るため、厳密な公式値が必要な場合は [BOAT RACE オフィシャル](https://www.boatrace.jp/) を確認してください。

### どうやって取得したか
1. 日次の全国結果 JSON を取得（負荷配慮のためリクエスト間隔は運用側で調整）
2. 同じ生データから津（9）・蒲郡（7）をフィルタし、全国月次／選手月次へ振り分け
3. 開催のあった日だけ `data/raw/<venue>/YYYY-MM-DD.json` として保存

※ 全国 raw の一括コミットは容量が大きいため、デフォルトでは gitignore 対象です。

### 整形表示（公式 RacketFrames）
事前に `raco pkg install --user --auto RacketFrames` が必要です。

```bash
mise run show:tsu
mise run show:gamagori
mise run show:national-month
# または
racket code/venue-racketframes-display.rkt tsu
racket code/venue-racketframes-display.rkt gamagori
racket code/national-month-summary.rkt 2026-08
```

- 本体: `code/venue-racketframes-display.rkt`
- 互換ラッパー: `code/tsu-racketframes-display.rkt` / `code/gamagori-racketframes-display.rkt`
- 出力例: `data/parsed_tsu_races.csv` / `output/tsu-racketframes-report.txt`


## 🛠️ 開発と検証
本書のサンプルコードおよびZenn/EPUBプレビュー環境は、タスクランナー `mise` を用いてローカル検証が可能です。

### 事前準備
```bash
# ツールのセットアップ
mise trust
mise install
npm install
```

### データ取得・パース・プロット・テスト実行
* **無確認本日データ取得**: `mise run data:download:today`
* **対話指定単日データ取得**: `mise run data:download:json`
* **対話指定過去期間一括蓄積**: `mise run data:download:range`
* **津日次 JSON の整形表示（最大約3年）**: `mise run show:tsu`
* **蒲郡日次 JSON の整形表示（最大約3年）**: `mise run show:gamagori`
* **全国月次集計サマリ**: `mise run show:national-month`
* **JSONパース&CSV全自動生成**: `mise run parse:json`
* **モーター相関散布図表示**: `mise run plot:scatter`
* **予想AI的中率答え合わせテスト**: `mise run model:predict`
* **全Racketスクリプト一括自動テスト**: `mise run test:racket`
* **Zennプレビュー（ローカルサーバー）**: `mise run book:preview`
